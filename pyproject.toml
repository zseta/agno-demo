[project]
name = "agno-scylladb-demo"
version = "0.1.0"
description = "Agno + ScyllaDB vector store integration demo"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "agno",
    "scylla-driver",
    "pypdf",
    "openai",
    "typer[all]",
]
